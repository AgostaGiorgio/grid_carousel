/// Class representing the size of the [GridCarousel] component.
class GridSize {
  const GridSize({required this.width, required this.height});

  /// Width assigned to the component.
  final double width;
  /// Height assigned to the component.
  final double height;
}
